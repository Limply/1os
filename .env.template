# 1OS Environment Configuration — copy to .env and fill in values
# Never commit .env to git

DEBUG=False
SECRET_KEY=change-me-to-a-long-random-string

DB_NAME=astronic
DB_USER=astronic_user
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

ALLOWED_HOSTS=ast1.sim-eng.com,localhost
CSRF_TRUSTED_ORIGINS=https://ast1.sim-eng.com
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Frontend (Vite) — server-specific, read by frontend/vite.config.js
# Each server sets its own port / API proxy target / allowed hosts here.
VITE_PORT=5173
VITE_API_TARGET=http://127.0.0.1:8000
VITE_ALLOWED_HOSTS=localhost

TELEGRAM_BOT_TOKEN=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
