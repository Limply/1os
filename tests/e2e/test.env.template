# 1OS E2E smoke test config — copy to test.env and fill in values.
# test.env matches the repo's *.env gitignore rule, so it never gets committed.

TEST_BASE_URL=https://1os.astronic.com.sg
TEST_EMAIL=tester@astronic.com.sg
TEST_PASSWORD=
